# Development configs
  `.emacs.d` is meant for emacs config. However, `etc` has bunch of scripts/configs to make life a bit easier and fun, so the one doesn't use Emacs can also appreciate the stuff I use daily.
