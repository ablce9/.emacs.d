[alias]
	l = log --pretty=oneline -n20 --graph --abbrev-commit
	s = status -s
	d = !"git diff-index --quiet HEAD -- || clear; git --no-pager diff --patch-with-stat"
	c = clone --recursive
	graph = log --graph --color --pretty=format:"%C(yellow)%H%C(green)%d%C(reset)%n%x20%cd%n%x20%cn%x20(%ce)%n%x20%s%n"
	amend = commit --amend --reuse-message=HEAD

[apply]
	whitespace = fix
[core]
	whitespace = space-before-tab,-indent-with-non-tab,trailing-space
	editor = emacs

[color]
	ui = auto

[color "branch"]

	current = yellow reverse
	local = yellow
	remote = green

[color "diff"]

	meta = yellow bold
	frag = magenta bold
	old = red
	new = green

[color "status"]

	added = yellow
	changed = green
	untracked = cyan

[user]
	email = shunsuketamiya@posteo.net
	name = Shunsuke Tamiya
	signingkey = B603407C78A189657BA842144E53D51F715DAA9D

[push]
	default = matching

[commit]
	gpgsign = true
